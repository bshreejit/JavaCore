package thirdDay.staticKeyword;

public class mainMethodAsStatic {

	public static void main(String[] args) {
		
		/* Main() is defined as"
		 * 1. public --->  so the compiler can access it
		 * 2. void ---> Cause all the operations are carried out
		 * 	  within main() so doesn't have to return anything.
		 * 3. Static ---> entry point of execution, and because
		 * 	  someone can create objects of mainMethodAsStatic class
		 * 	  and the objects(can be multiple) can call the method 
		 *    main() more than once.So to avoid multiple main()
		 *    executed at the same time
		 */
		
		
	}

}
